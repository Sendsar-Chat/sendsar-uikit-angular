# Changelog

## 0.1.1

- Message list: in-memory per-room thread cache for instant conversation switches
- Message list: use `createRoomSubscription` `meta.nextCursor` (requires `@sendsar/chat-sdk-javascript` ≥ 0.2.2) — removes duplicate first-page history fetch

## 0.1.0

- Initial release
